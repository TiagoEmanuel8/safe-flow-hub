apiVersion: v1
kind: Service
metadata:
  name: {{ include "safe-flow-app.fullname" . }}
  labels:
    {{- include "safe-flow-app.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "safe-flow-app.selectorLabels" . | nindent 4 }}
